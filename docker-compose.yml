version: '3.8'

services:
  app:
    build: .
    image: finally
    container_name: finally
    ports:
      - "8000:8000"
    volumes:
      - finally-data:/app/db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  finally-data:
