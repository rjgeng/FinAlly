{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if [ -n \"$FINALLY_REVIEW_HOOK_RUNNING\" ]; then exit 0; fi; export FINALLY_REVIEW_HOOK_RUNNING=1; codex exec \"Review changes since last commit and write results to a file named planning/REVIEW.md\"'"
          }
        ]
      }
    ]
  }
}